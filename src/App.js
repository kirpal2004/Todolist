import React from "react";
import ReactDOM from "react-dom/client";
import Mybutton from "./App"; // ✅ corrects the import

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Mybutton />);
